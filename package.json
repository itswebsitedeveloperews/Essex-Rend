{
  "name": "essex-rend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ASTRO_DEV_BACKGROUND=0 astro dev --host 0.0.0.0 --port 5000",
    "build": "astro build",
    "preview": "ASTRO_PREVIEW_BACKGROUND=0 astro preview --host 0.0.0.0 --port 5000",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.0.0",
    "astro": "^7.2.8"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
